optimizer: adamw
learning_rate: 1e-3
weight_decay: 1e-4
epochs: 60
patience: 10
grad_clip_norm: 1.0
loss: bce          # bce | focal | focal_conf  (BCE is best on this dataset)
seeds: [42]
calibration: temperature
