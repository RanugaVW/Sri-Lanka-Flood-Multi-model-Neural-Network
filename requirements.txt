# Core
torch>=2.1
torch-geometric>=2.5
torchvision>=0.16

# Data
pandas>=2.0
numpy>=1.24
pyarrow>=14.0        # parquet support for panel.parquet

# Baselines / trees
scikit-learn>=1.3
lightgbm>=4.0

# Calibration
netcal>=1.3          # temperature scaling / ECE helpers (optional; hand-rolled versions also fine)

# Config + experiment tracking
pyyaml>=6.0
tqdm>=4.66

# Imagery
rasterio>=1.3        # if reading SAR chips as GeoTIFF; skip if chips are pre-converted to .npy

# Testing
pytest>=7.4
